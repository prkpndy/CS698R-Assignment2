'''Setup instructions for all environments.

This file shouldn't need to be changed unless adding external dependencies or bumping the version number.
'''
from setuptools import setup

setup(
    name='environments',
    version='1.0.0',
    install_requires=['gym']
)
